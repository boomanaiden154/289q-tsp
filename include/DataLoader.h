// The header file containing the code that loads the data from the provided
// text files.

#include <map>
#include <string>
#include <utility>

std::map<std::pair<int, int>, float> loadDataFromFile(std::string FilePath);
